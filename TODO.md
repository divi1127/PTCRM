# TODO
- [x] Fix compile error in `client/src/pages/employee/EmployeeTargets.jsx` (unexpected token parse error)
- [ ] Re-check AdminTargets.jsx for similar issues (safe null-guards + property normalization) after employee page compiles
- [ ] Run `npm test`/`npm run build` or equivalent to confirm no syntax errors

